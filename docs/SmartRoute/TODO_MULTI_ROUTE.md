# 待办事项清单 (TODO) - 多策略选线阶段

## 1. 立即执行 (Immediate)
- **前端清理**: `RouteForm.vue` 中的 `routeResult` prop 定义未被使用，建议在下次提交时删除，保持代码整洁。
- **验证**: 请手动在浏览器中测试完整流程（输入起终点 -> 规划 -> 切换路线 -> 确认地图变化），确保交互流畅。

## 2. 下一步开发 (Next Phase)
- **数据导出 (Data Export)**:
  - 需求: 将选定的路线数据（包含路径点、车辆参数）导出为 JSON 或 PDF。
  - 目的: 交付给 "桥梁效应比值计算" 团队进行物理通过性验算。
- **智能问答 (SmartQA)**:
  - 状态: 仅有 UI 框架，尚未接入后端逻辑。
  - 计划: 实现基于路线数据的简单的问答功能（如 "这条路隧道多吗？"）。

## 3. 配置检查 (Configuration)
- **API Key**: 确保 `.env` 中的 `VITE_AMAP_KEY` (Web JS) 和后端 `.env` 中的 `AMAP_API_KEY` (Web Service) 均有效且额度充足。
- **车辆数据**: 确保车辆档案（长宽高重）准确，因为这直接影响高德 API (即使是标准驾车模式，我们也在逻辑层面做了筛选或预留了扩展)。

## 4. 已知限制
- **限行数据**: 目前使用的是标准驾车/货车 API (取决于 Key 权限)，未购买高级货车路径规划，因此 "限行/禁行" 信息可能不完全准确，需依赖人工校验或后续对接外部系统。
